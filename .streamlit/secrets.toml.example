# Streamlit Secrets Configuration
# Copy this file to secrets.toml and fill in your values
# For Streamlit Cloud deployment, set these in the app settings

# PyST API Configuration
PYST_HOST = "https://vocab.sentier.dev/api/v1/"
PYST_AUTH_TOKEN = ""  # Leave empty if authentication is not required
PYST_TIMEOUT = "30"
